/*import {
  it,
  iit,
  describe,
  ddescribe,
  expect,
  inject,
  injectAsync,
  TestComponentBuilder,
  beforeEachProviders
} from 'angular2/testing';
import {provide} from 'angular2/core';
import {TimeToDate} from './time-to-date';


describe('TimeToDate Pipe', () => {

  beforeEachProviders(() => [TimeToDate]);


  it('should transform the input', inject([TimeToDate], (pipe:TimeToDate) => {
      expect(pipe.transform(true)).toBe(null);
  }));

});
*/